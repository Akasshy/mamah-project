// File app_colors.dart (diperbarui)
import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFFFCCCC);
  // 🔵 Ganti hijau jadi biru dengan variasi tone
  static const Color inputFill = Color(
    0xFFE3F2FD,
  ); // Biru sangat muda (background input)
  static const Color inputBorder = Color(
    0xFF90CAF9,
  ); // Biru muda (border normal)
  static const Color inputBorderFocused = Color(
    0xFF1565C0,
  ); // Biru gelap (border fokus)
  static const Color iconColor = Color(0xFF1565C0); // Biru gelap (ikon)

  static const Color buttonBackground = Color(0xFF5199CB); // Biru utama
  static const Color buttonText = Colors.white;

  static const Color labelText = Color(0xFF546E7A); // Biru keabu-abuan
  static const Color linkColor = Color(
    0xFF1976D2,
  ); // Biru sedikit terang (link)

  static const Color primary = Color(0xFF5199CB); // Biru utama

  // Warna teks
  static const Color primaryTextColor = Color(
    0xFF0D47A1,
  ); // Biru tua untuk teks utama
  static const Color secondaryTextColor = Color(
    0xFF455A64,
  ); // Biru abu untuk teks sekunder
  static const Color hintTextColor = Color(
    0xFF90A4AE,
  ); // Biru-abu muda untuk hint
}
